id: core_geography
name: Core Geography & Census Baseline
version: 1.1.0
status: active
description: "Stable place identity, Census baseline, administrative hierarchy and temporal source crosswalk foundation for every JLA module."
geography:
  primary: place
  supports: [state, district, subdistrict, block, panchayat, village, town]
temporal:
  type: versioned_reference
  views: [census_2011, current_lgd]
dependencies: []
features:
  map: true
  download: true
  report: true
  comparison: true
  provenance: true
coverage:
  bundled: "24 Jharkhand districts (verified Census 2011 baseline)"
  source_inventory: "authoritative Census 2011 + DCHB + OGD PCA + LGD + Jharkhand block references verified"
  full_village_ingest: pending_official_raw_files
  target: "authoritative state/district/subdistrict/block/panchayat/village/town crosswalk with Census 2011 demographic baseline and current LGD temporal view"
  note: "No village/block/panchayat record is fabricated; full publication occurs only after official raw files are ingested, checksummed and validated."
