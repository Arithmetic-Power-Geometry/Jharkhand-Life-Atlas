# Copy this template into Streamlit Cloud -> App settings -> Secrets.
# Never commit real production credentials.
[admin]
username = "your-admin-username"
password = "use-a-strong-unique-password"

# Reserved for a future direct-to-GitHub publisher.
# [github]
# repository = "owner/repository"
# branch = "main"
# token = "github_pat_..."
